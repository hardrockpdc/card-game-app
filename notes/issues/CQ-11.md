---
id: CQ-11
type: quality
area: ui
status: fixed
severity: low
opened: 2026-05-17
verified: 2026-08-15
evidence: "screens/RummyVariantPickerScreen.js (138 lines) and its direct collaborators (game/rummy.js, components/VariantOptionGrid.js, components/GameSetupLayout.js) all have zero conquian/Conquian references, case-insensitive grep"
---

## Problem

**CQ-11 — RESOLVED (verified 2026-06-18).** No Conquián references remain
in `RummyVariantPickerScreen` — the half-wired state is gone.

(Checklist-line summary — fuller v2 writeup, describing what the original "half-wired
Conquián state" bug actually looked like, was deleted from the repo. The original bug's
exact shape could not be reconstructed; only the narrow, verifiable claim — zero
references remain today — was checked.)

## Verified 2026-08-15

`screens/RummyVariantPickerScreen.js` still exists under that exact name (not renamed) —
a clean, narrowly-scoped 138-line screen that picks a Rummy variant from
`RUMMY_VARIANT_OPTIONS`, builds AI players, and navigates to `RummyGame` or hands the
choice back to the Lobby. Zero matches for `conquian`/`Conquian` (case-insensitive) in
this file or its direct collaborators (`game/rummy.js`, `components/VariantOptionGrid.js`,
`components/GameSetupLayout.js`). No mode branching, dead state, or leftover props
referencing Conquián anywhere. A repo-wide grep confirms Conquián now lives entirely in
its own dedicated files (`ConquianSetupScreen.js`, `ConquianGameScreen.js`,
`game/conquian.js`, etc.) — none of those hits fall inside the Rummy picker or its
imports.

Verdict is `fixed` for the narrow, verifiable claim (zero Conquián references remain
today). It is not a verification against the original bug's deeper mechanics, since
those were never recoverable.
