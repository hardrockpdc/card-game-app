---
id: CQ-10
type: quality
area: data
status: fixed
severity: low
opened: 2026-05-17
verified: 2026-08-15
evidence: "game/lastCard.js:177-217 (applyCard), :219-238 (chooseColor), :240-260 (drawCard) all build new state via spread, no in-place mutation; __tests__/lastCard.actions.test.js:111-159 purity suite, 13/13 passing; commit 2bbd262 (2026-06-18) resolves the archive's own unfilled <this commit> placeholder, touches only the test file"
---

## Problem

**CQ-10 — DONE 2026-06-18 (`<this commit>`).** The `lastCard.js` action
functions (`applyCard`/`drawCard`/`chooseColor`) already build new state via
spreads and never mutate input; added purity tests in
`__tests__/lastCard.actions.test.js` (deep-compare input before/after) to lock
it in. 297 tests pass.

(Checklist-line summary — fuller v2 writeup deleted from the repo. Note the literal
placeholder `<this commit>` left unfilled in the source text — a real gap in the original
doc, resolved below.)

## Verified 2026-08-15

Confirmed against today's code. All three functions build new state via spread, no
mutation: `applyCard` (`game/lastCard.js:177-217`) filters/spreads into `newHand`/
`newDiscardPile`/`nextState`, no `.push`/`.splice`/direct assignment on input;
`chooseColor` (`:219-238`) returns `{...state, ...}`; `drawCard` (`:240-260`) uses array
destructuring (non-mutating) and returns a spread-based state including a nested spread
for `hands`.

The purity tests exist and pass exactly as described: `__tests__/lastCard.actions.test.js
:111-159`, deep JSON-snapshot-compare before/after, covering `applyCard` (3 variants),
`drawCard`, and `chooseColor`. `npx jest __tests__/lastCard.actions.test.js` — 13/13 pass
today. Full suite is 567/567 across 47 suites (the doc's "297 tests" figure is a stale
snapshot from that day, not a meaningful current health check — noted, not treated as a
discrepancy).

The archive's own unfilled `<this commit>` placeholder is now resolved: commit
`2bbd262e277276c4da1d531c49d5dec44bd7478a`, "test(lastcard): lock in action-function
purity (CQ-10)," dated 2026-06-18 — matches the doc's "DONE 2026-06-18" exactly. The
commit touched only the test file (52 lines added), not `game/lastCard.js` — consistent
with the claim that purity was pre-existing and this commit only added tests to lock it
in. Claim fully accurate, no fix needed.
