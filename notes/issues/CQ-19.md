---
id: CQ-19
type: quality
area: build
status: open
severity: low
opened: 2026-08-19
verified: 2026-08-19
evidence: "`git add --renormalize .` on 2026-08-19 staged 123 files; `git show HEAD:review-screenshots/README.md | od -c` shows CRLF bytes in the committed blob while `.gitattributes` declares `*.md text eol=lf`, so git wants LF in the blob and every text file compares dirty once the index stat cache is invalidated"
---

## Problem

`.gitattributes` declares `*.js text eol=crlf` and `*.md text eol=lf`, but the files were
never renormalized after it was added. The committed blobs still carry CRLF, so git wants
to rewrite every text file's stored line endings. `git status` normally hides this behind
the index stat cache; any operation that invalidates that cache (`git add --renormalize`,
some checkouts) makes all 123 files appear modified at once.

Nothing is actually wrong with the working tree. File contents and mtimes are untouched —
the diff is line endings only.

## Why it matters

Two live hazards:

1. **`git add -A` would sweep all 123 files into one churn commit.** Use explicit paths in
   this repo until this is fixed.
2. `git checkout -- <file>` does not clear the dirty state for an affected file, which
   makes it look like a tool is rewriting the file in the background. It isn't.

## Fix

The git-prescribed one-time fix is `git add --renormalize .` followed by a single commit.
Two things must be settled first:

- `archive/PROJECT_NOTES.md` is in the affected set, and CLAUDE.md freezes that file —
  never edit it. The renormalize must exclude it, or the freeze rule has to be reconciled
  with a content-identical line-ending rewrite.
- The git history records a deliberate past incident fix ("style: format code with
  Prettier, restore CRLF line endings"). Confirm renormalizing stores LF in the blobs and
  still checks out CRLF on disk per `eol=crlf`, so the incident does not recur.

Not urgent, but it should be one focused session rather than a bolt-on to unrelated work.

## History

- **2026-08-19** — Found while clearing a phantom-modified `review-screenshots/README.md`
  before a push. Diagnosed, deliberately not fixed; see [[2026-08-19]].
