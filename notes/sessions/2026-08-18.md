---
verified: 2026-08-18
---

# 2026-08-18

## Changed
- `notes/product/Post-Launch Checklist.md` — rules-republish item checked off (reported
  2026-08-18); the "Known open items" Firebase entry and the 2-device retest item reworded
  so the retest is named as the only thing that can confirm the deploy. Dead root-doc
  citations (`DATABASE_RULES.md`, `IOS_SETUP.md`, `GAME_ROADMAP.md`) repointed at notes.
- `notes/architecture/Security Model.md`, `notes/architecture/Project Facts.md`,
  `notes/ops/iOS Setup.md`, `notes/ops/Build and Release.md` — all four claimed the
  2026-08-02 rules fixes were unpublished; now say republished-but-unconfirmed. Also
  fixed Project Facts' claim that root keeps only `README.md` + `CLAUDE.md` (it also
  keeps the Impeccable files `PRODUCT.md` + `DESIGN.md`).
- `notes/product/Roadmap.md` — `COIN_ECONOMY.md` citation repointed at [[Coin Economy]].
- `CLAUDE.md`, `notes/00 Index.md` — `notes/sessions/` now exists, so both stop
  describing it as not-yet-created.

## Issues
- [[LAUNCH-3]] — stays `open`/high, `verified: 2026-08-18`. Rules republish reported done;
  the stale production build (versionCode 9 never built, `sentryDsn` still null) and the
  unconfirmed deploy keep it open. New "Update 2026-08-18" section records both halves.
- [[LAUNCH-2]], [[BUG-5]], [[CQ-13]] — no status change; dead `CLAUDE.md:<line>` citations
  replaced with section refs or with the note that actually holds the fact now.

## Decisions
- Recorded the Firebase republish as **reported, not verified**, everywhere it appears.
  The console is the only witness and the repo cannot read live rules back; the paired
  2-device retest is what confirms it. Chose this over checking the item off as done,
  because a bad rules deploy breaks private hands specifically and claiming-live-when-dead
  is the dangerous direction of the error.
- Cited `CLAUDE.md` by **section number**, not line number. Line-number citations rotted
  once already when CLAUDE.md was cut from 31 KB to 136 lines.
- Started `notes/sessions/` with this entry rather than editing CLAUDE.md to drop the
  `@notes/sessions/` import — the import was the only genuinely broken reference, and the
  directory was always the intended destination.

## Next
Run the 2-device online multiplayer retest — it confirms the rules deploy and is the top
blocker in [[LAUNCH-3]]. Deal a poker hand, confirm it reaches the right player and no one
else's hand is visible.

## Open questions
- Nothing blocking. After the retest: does `expo.extra.sentryDsn` get set before the
  versionCode 9 build, or does that build ship with crash reporting still a no-op?
